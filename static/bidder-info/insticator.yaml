endpoint: "https://ex-v2.hunchme.com/v1/prebidserver?debug=true"
endpointCompression: GZIP
maintainer:
  email: "prebid@insticator.com"
gvlVendorID: 910
openrtb:
  version: "2.6"
capabilities:
  app:
    mediaTypes:
      - banner
      - video
      - audio
  site:
    mediaTypes:
      - banner
      - video
      - audio
userSync:
  iframe:
    url: "https://usync.hunchme.com?gdpr={{.GDPR}}&gdpr_consent={{.GDPRConsent}}&us_privacy={{.USPrivacy}}&redirect={{.RedirectURL}}"
    userMacro: "$UID"
